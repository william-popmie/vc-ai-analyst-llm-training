pandas>=2.0
scikit-learn>=1.4
numpy>=1.24
joblib>=1.3

# ONNX export (export_onnx.py only). onnx is pinned <1.22 because newer
# releases reject the boolean tree attributes skl2onnx emits.
skl2onnx>=1.20
onnx==1.18.0
onnxruntime>=1.20  # used only to verify the exported model
